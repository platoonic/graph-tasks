import styled from "styled-components";

const Container = styled.div`
  background-color: blue;
  color: white;
  padding: 20px;
`;

const TaskCard = ({}) => {
  return <Container>hello task card</Container>;
};

export default TaskCard;
